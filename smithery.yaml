# Compatibility manifest for clients and legacy Smithery importers.
# The canonical Smithery listing is published as a remote Streamable HTTP server.
startCommand:
  type: http
  config:
    url: https://mcp.xiaoflow.com/mcp

configSchema:
  type: object
  additionalProperties: false
  properties: {}
